#!/bin/bash
# =============================================================================
# Platform: WASM (短名称别名，指向 browser)
# =============================================================================
# 支持通过 --platform wasm 快速加载 browser 平台配置
# =============================================================================

_PLATFORM_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${_PLATFORM_DIR}/browser.sh"
unset _PLATFORM_DIR
