#!/bin/bash
# =============================================================================
# Platform: Windows (短名称别名)
# =============================================================================
# 此文件是 windows-msys2.sh 的别名，支持通过 --platform windows 快速加载
# =============================================================================

_PLATFORM_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${_PLATFORM_DIR}/windows-msys2.sh"
unset _PLATFORM_DIR
